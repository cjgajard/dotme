case $- in
  *i*) ;;
  *) return;;
esac

# SETUP
# -----
library=(git)
plugin=()
smart_plugin=false
theme="dual"
. $HOME/cjgajard/configfiles/bash/loader.sh

# BASH
# ----
shopt -s histappend
HISTCONTROL=ignoreboth
