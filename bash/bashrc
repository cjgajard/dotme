case $- in
  *i*) ;;
  *) return;;
esac

# BASH
# ----
HISTFILE=
HISTCONTROL=ignoreboth:erasedups
shopt -s checkwinsize
shopt -s histappend
shopt -s histverify

# SETUP
# -----
lib=(git)
plugin=(user smart med ls)
theme="min"

export ME_DIR=$HOME/.me
. $ME_DIR/bash/loader.sh
