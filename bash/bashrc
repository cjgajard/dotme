case $- in
  *i*) ;;
  *) return;;
esac

# SETUP
# -----
library=(user ps git sunwait)
plugin=(user ls mancolor)
smart_plugin=true
theme="monal"
. $(dirname `readlink -f ${BASH_SOURCE}`)/loader.sh

# BASH
# ----
shopt -s histappend
HISTCONTROL=ignoreboth:erasedups
