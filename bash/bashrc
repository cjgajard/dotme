case $- in
  *i*) ;;
  *) return;;
esac

# SETUP
# -----
library=(git ps)
plugin=(ls user ripley)
smart_plugin=true
theme="monal"
. $HOME/cjgajard/configfiles/bash/loader.sh

# BASH
# ----
shopt -s histappend
HISTCONTROL=ignoreboth
