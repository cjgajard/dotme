case $- in
  *i*) ;;
  *) return;;
esac

# SETUP
# -----
library=(user ps git sunwait)
plugin=(user ls)
smart_plugin=true
theme="monal"
. $HOME/cjgajard/configfiles/bash/loader.sh

# BASH
# ----
shopt -s histappend
HISTCONTROL=ignoreboth:erasedups
