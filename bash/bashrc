case $- in
  *i*) ;;
  *) return;;
esac

# BASH
# ----
HISTCONTROL=ignoreboth:erasedups
HISTSIZE=1000
shopt -s checkwinsize
shopt -s histappend

# SETUP
# -----
lib=(user git)
plugin=(user kak smart ls mancolor)
theme="monal"
. $(dirname `readlink -f ${BASH_SOURCE}`)/loader.sh
