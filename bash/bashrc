case $- in
  *i*) ;;
  *) return;;
esac

# SETUP
# -----
export REPO="$HOME"

library=(git)
plugin=()
smart_plugin=false
theme="dual"
. $REPO/cjgajard/configfiles/bash/loader.sh

# BASH
# ----
shopt -s histappend
HISTCONTROL=ignoreboth
