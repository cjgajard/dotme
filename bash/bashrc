case $- in
  *i*) ;;
  *) return;;
esac

# SETUP
# -----
library=(git ps)
plugin=(nvim user)
smart_plugin=true
theme="monal"
. $HOME/cjgajard/configfiles/bash/loader.sh

# BASH
# ----
shopt -s histappend
HISTCONTROL=ignoreboth
