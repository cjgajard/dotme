# SETUP
# -----
bashrc_sourcefile=$(readlink -f ${BASH_SOURCE})
export CARLSH="${bashrc_sourcefile%/*/*}"

library=(git mfind ruby)
plugin=(autojump colorauto ls)
smart_plugin=true
theme="dual"
source $CARLSH/bash/loader.sh

# BASH SETTING
# ------------
shopt -s histappend
HISTCONTROL=ignoreboth
HISTSIZE=1000
